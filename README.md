# MusicLibrary
Music Streaming Library (Java) | Computer Science Project (Linked Lists)

Developed a Java music backend, parsing 7 distinct CSV datasets to centralize over 100 individual song objects.

Engineered circular linked lists and arrays for playlist management, achieving O(1) node insertions and deletions.

Implemented algorithms to merge popularity-sorted playlists and execute O(n) randomized shuffling.
